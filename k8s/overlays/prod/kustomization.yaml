namePrefix: prod-
commonLabels:
  variant: production
bases:
- ../../base/
patchesStrategicMerge:
- deployment.yaml
- service.yaml
- pdf-deployment.yaml
configMapGenerator:
- name: config
  behavior: merge
  files:
    - local.yml
